spring:
  application:
    name: item-service # 服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: shared-jdbc.yml # 共享mybatis配置
          - dataId: shared-logs.yml # 共享日志配置
          - dataId: shared-knife4j.yml # 共享日志配置
          - dataId: shared-seata.yaml # 共享seata配置