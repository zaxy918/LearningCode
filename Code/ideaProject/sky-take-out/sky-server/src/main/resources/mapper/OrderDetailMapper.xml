<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">
    <insert id="insertBatch">
        insert into sky_take_out.order_detail (order_id, dish_id, setmeal_id, name, image, dish_flavor, number, amount)
        values
        <foreach collection="orderDetails" item="item" separator=",">
            (#{item.orderId}, #{item.dishId}, #{item.setmealId}, #{item.name}, #{item.image}, #{item.dishFlavor},
            #{item.number}, #{item.amount})
        </foreach>
    </insert>
</mapper>
