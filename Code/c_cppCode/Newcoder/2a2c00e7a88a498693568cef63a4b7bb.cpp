#include <iostream>
using namespace std;

const int MAX_N = 1e7 + 1;

long long stack[MAX_N];
long long arr[MAX_N], left[MAX_N], right[MAX_N];
int top;

int main() {
  std::ios::sync_with_stdio(false);
  int n;
  cin >> n;
  for (int i = 0; i < n; i++) {
    cin >> arr[i];
  }
  for (int i = 0; i < n; i++) {
    }
  return 0;
}