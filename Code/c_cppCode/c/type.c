#include <stdbool.h>
#include <stdio.h>
int main() {
  int a = 1;
  // if (a == true && !false)
  //   printf("a is true\n");
  // else
  //   printf("a is false\n");

  // do {
  //   printf("in do while\n");
  // } while (false);

  // for (int i = 0; i < 3; i++) {
  //   for (int j = 0; j < 2; j++) {
  //     printf("i = %d, j = %d\n", i, j);
  //   }
  // }

  // int b = a++;
  // // b = a;
  // // a += 1;
  // int c = ++a;
  // // a += 1;
  // // c = a;
  // printf("a = %d, b = %d, c = %d\n", a++, b, c);
  // if (!a == 0 || a++) {
  //   printf("a = %d\n", a);
  // }
  // printf("a = %d\n", a);
  return 0;
}